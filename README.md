# pathman
A small tool that lets you run your scripts from anywhere using simple commands. Instead of jumping between folders, you give your scripts short names and call them directly from the terminal. It handles setup quietly in the background and stays out of your way once it’s in place.
